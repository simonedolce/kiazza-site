{% macro fileInput(inputElement) %}

    <div class="file field has-name is-fullwidth">
        <label class="file-label">
            {{ form_widget(inputElement) }}
            <span class="file-cta">
            <span class="file-icon">
              <i class="fas fa-upload"></i>
            </span>
            <span class="file-label">
              Choose a file…
            </span>
        </span>

            <span class="file-name">
            <p class="headline avatar-file-name"></p>
        </span>

        </label>
    </div>
{% endmacro %}